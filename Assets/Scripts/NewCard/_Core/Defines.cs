namespace Cardevil.NewCard.Core
{
    public enum CardType
    {
        Attack, 
        Move    
    }
    
    public enum CardColor : byte
    {
        None,
        Red,
        Green,
        Blue,
        Black
    }
}